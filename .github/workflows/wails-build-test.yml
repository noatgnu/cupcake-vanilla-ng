name: Wails Build and Test

on:
  push:
    branches: [ master ]
    paths:
      - 'cupcake-vanilla-wails/**'
      - '.github/workflows/wails-build-test.yml'
  pull_request:
    branches: [ master ]
    paths:
      - 'cupcake-vanilla-wails/**'
      - '.github/workflows/wails-build-test.yml'

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: cupcake-vanilla-wails/go.sum

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Install root dependencies
        run: npm ci

      - name: Build cupcake-core library
        run: npx ng build @noatgnu/cupcake-core

      - name: Install Wails frontend dependencies
        working-directory: ./cupcake-vanilla-wails/frontend
        run: npm ci

      - name: Build setup frontend
        working-directory: ./cupcake-vanilla-wails/frontend
        run: npm run build

      - name: Build main Angular app
        run: npx ng build --configuration=wails || npx ng build --configuration=production

      - name: Copy main app to embed directory
        run: |
          mkdir -p cupcake-vanilla-wails/mainapp/dist/browser
          if [ -d "dist/cupcake-vanilla-ng/browser" ]; then
            cp -r dist/cupcake-vanilla-ng/browser/* cupcake-vanilla-wails/mainapp/dist/browser/
          elif [ -d "dist/browser" ]; then
            cp -r dist/browser/* cupcake-vanilla-wails/mainapp/dist/browser/
          else
            echo "<html><body>Placeholder</body></html>" > cupcake-vanilla-wails/mainapp/dist/browser/index.html
          fi

      - name: Run Go tests
        working-directory: ./cupcake-vanilla-wails
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./cupcake-vanilla-wails/coverage.out
          flags: wails
        continue-on-error: true

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: cupcake-vanilla-wails/go.sum

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v3/cmd/wails3@latest

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Install root dependencies
        run: npm ci

      - name: Build cupcake-core library
        run: npx ng build @noatgnu/cupcake-core

      - name: Install Wails frontend dependencies
        working-directory: ./cupcake-vanilla-wails/frontend
        run: npm ci

      - name: Build setup frontend
        working-directory: ./cupcake-vanilla-wails/frontend
        run: npm run build

      - name: Build main Angular app
        run: npx ng build --configuration=wails || npx ng build --configuration=production

      - name: Copy main app to embed directory
        run: |
          mkdir -p cupcake-vanilla-wails/mainapp/dist/browser
          if [ -d "dist/cupcake-vanilla-ng/browser" ]; then
            cp -r dist/cupcake-vanilla-ng/browser/* cupcake-vanilla-wails/mainapp/dist/browser/
          elif [ -d "dist/browser" ]; then
            cp -r dist/browser/* cupcake-vanilla-wails/mainapp/dist/browser/
          fi

      - name: Generate Wails bindings
        working-directory: ./cupcake-vanilla-wails
        run: |
          export PATH="$HOME/go/bin:$PATH"
          wails3 generate bindings

      - name: Build Wails app
        working-directory: ./cupcake-vanilla-wails
        run: |
          mkdir -p build/bin/linux-amd64
          go build -ldflags="-s -w" -o build/bin/linux-amd64/cupcake-vanilla .

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: cupcake-vanilla-linux-amd64
          path: cupcake-vanilla-wails/build/bin/linux-amd64/
          retention-days: 7

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: cupcake-vanilla-wails/go.sum

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v3/cmd/wails3@latest

      - name: Install root dependencies
        run: npm ci

      - name: Build cupcake-core library
        run: npx ng build @noatgnu/cupcake-core

      - name: Install Wails frontend dependencies
        working-directory: ./cupcake-vanilla-wails/frontend
        run: npm ci

      - name: Build setup frontend
        working-directory: ./cupcake-vanilla-wails/frontend
        run: npm run build

      - name: Build main Angular app
        run: npx ng build --configuration=wails || npx ng build --configuration=production
        continue-on-error: true

      - name: Copy main app to embed directory
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path cupcake-vanilla-wails/mainapp/dist/browser
          if (Test-Path dist/cupcake-vanilla-ng/browser) {
            Copy-Item -Recurse -Force dist/cupcake-vanilla-ng/browser/* cupcake-vanilla-wails/mainapp/dist/browser/
          } elseif (Test-Path dist/browser) {
            Copy-Item -Recurse -Force dist/browser/* cupcake-vanilla-wails/mainapp/dist/browser/
          } else {
            "<html><body>Placeholder</body></html>" | Out-File -FilePath cupcake-vanilla-wails/mainapp/dist/browser/index.html
          }

      - name: Generate Wails bindings
        working-directory: ./cupcake-vanilla-wails
        run: wails3 generate bindings

      - name: Build Wails app
        working-directory: ./cupcake-vanilla-wails
        run: |
          New-Item -ItemType Directory -Force -Path build/bin/windows-amd64
          go build -ldflags="-s -w -H windowsgui" -o build/bin/windows-amd64/cupcake-vanilla.exe .
        shell: pwsh

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: cupcake-vanilla-windows-amd64
          path: cupcake-vanilla-wails/build/bin/windows-amd64/
          retention-days: 7

  build-macos:
    name: Build macOS
    runs-on: macos-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: cupcake-vanilla-wails/go.sum

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v3/cmd/wails3@latest

      - name: Install root dependencies
        run: npm ci

      - name: Build cupcake-core library
        run: npx ng build @noatgnu/cupcake-core

      - name: Install Wails frontend dependencies
        working-directory: ./cupcake-vanilla-wails/frontend
        run: npm ci

      - name: Build setup frontend
        working-directory: ./cupcake-vanilla-wails/frontend
        run: npm run build

      - name: Build main Angular app
        run: npx ng build --configuration=wails || npx ng build --configuration=production
        continue-on-error: true

      - name: Copy main app to embed directory
        run: |
          mkdir -p cupcake-vanilla-wails/mainapp/dist/browser
          if [ -d "dist/cupcake-vanilla-ng/browser" ]; then
            cp -r dist/cupcake-vanilla-ng/browser/* cupcake-vanilla-wails/mainapp/dist/browser/
          elif [ -d "dist/browser" ]; then
            cp -r dist/browser/* cupcake-vanilla-wails/mainapp/dist/browser/
          else
            echo "<html><body>Placeholder</body></html>" > cupcake-vanilla-wails/mainapp/dist/browser/index.html
          fi

      - name: Generate Wails bindings
        working-directory: ./cupcake-vanilla-wails
        run: |
          export PATH="$HOME/go/bin:$PATH"
          wails3 generate bindings

      - name: Build Wails app (arm64)
        working-directory: ./cupcake-vanilla-wails
        run: |
          mkdir -p build/bin/darwin-arm64
          CGO_ENABLED=1 GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o build/bin/darwin-arm64/cupcake-vanilla .

      - name: Build Wails app (amd64)
        working-directory: ./cupcake-vanilla-wails
        run: |
          mkdir -p build/bin/darwin-amd64
          CGO_ENABLED=1 GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o build/bin/darwin-amd64/cupcake-vanilla .
        continue-on-error: true

      - name: Upload macOS arm64 artifact
        uses: actions/upload-artifact@v4
        with:
          name: cupcake-vanilla-macos-arm64
          path: cupcake-vanilla-wails/build/bin/darwin-arm64/
          retention-days: 7

      - name: Upload macOS amd64 artifact
        uses: actions/upload-artifact@v4
        if: success() || failure()
        with:
          name: cupcake-vanilla-macos-amd64
          path: cupcake-vanilla-wails/build/bin/darwin-amd64/
          retention-days: 7
          if-no-files-found: ignore
